# App feedback triage — 2026-09-10

49 feedback items from the latest testing round (48 numbered + share links, numbered #49 here), each with a short product-level action. Reporter attributions kept from the source list.

**Legend**
- **DECIDE** — open product call; a recommendation is given, review confirms: #2, #24, #26, #29, #34.
- **❓** — action is a best guess; open question at the bottom: #3, #17, #20.
- Carried-over repeats from the 2026-09-03 triage (`docs/app-feedback-todos.md`): #32 part 3 (↔ 9/3 #62), #41 (↔ 9/3 #64), #46 (↔ 9/3 #34/#50), #48 (↔ 9/3 #89).

| # | Feedback | What to do | Area |
|---|----------|------------|------|
| 1 | Switching domain name and naming across the platform: Switch to app.onhomeroom.com (since my.onhomeroom.com is confusing grammatically), update appchip name and all instances/weos references to Homeroom, and update the app icon + name to the new ones. (Evan, SNM) | Move the domain to app.onhomeroom.com with redirects from the old one; rename every weos/instance reference and the app chip to Homeroom; ship the new app icon + name. | Branding |
| 2 | Join waitlist screen & process: Landing page from PWA and mobile app should possibly match sign in with a join button, or have a short 1-line pitch linking out (check apps like Aave); landing page/step 1 is text-heavy and needs rewriting/simplification ("Describe the app you want in chat…"); back button overlaps text in step 1; https://my.onhomeroom.com/#waitlist?confirm=1 should be 2 steps instead of 1; missing space before "Optional" next to country name, and format/spacing for "*" needs improvement. (Evan, Zura, SNM, Lukas) | **DECIDE** landing layout — rec sign-in-style screen with a Join CTA plus a one-line pitch (Aave-style). Also: rewrite step 1 copy; fix the back-button overlap; split confirm=1 into 2 steps; fix the "Optional" and "*" spacing. | Waitlist |
| 3 | App theme background color: Theme background color looks skin-colored vs solarized-light. (Evan) | ❓ Retune the background away from the skin-toned tint toward a true solarized-light. See question below. | Core UI |
| 4 | Logout navigation: No quick or easy way to logout. (Evan) | Add an easy-to-find logout (profile/menu), one tap from anywhere. | Auth |
| 5 | Email confirmation: Confirmation email ("confirm your email") is not being received. (Evan, Lukas) | Fix confirmation-email delivery end to end (send, provider, spam placement). Blocker for the waitlist flow. | Emails |
| 6 | Performance & loading times: Initial homepage load on mobile is very slow every time the app opens, opening other apps from there is slow, spinning up/creating new apps takes very long (5+ mins or stuck in "spinning up"), and pulling up the keyboard on Android has low FPS / laggy animation. (Bruno, Lukas, Zura, SNM) | Treat as a workstream, not one fix: profile and fix cold homepage load, app-open latency, app-creation spin-up (5+ min / stuck), and Android keyboard animation FPS. | Performance |
| 7 | Card behavior, controls, and expansion: Expanding/clicking cards across Workshop vs other views is inconsistent—Workshop shows extra controls like "Reply" input, "Open on its own page", and recent comments while other views do not. Clicking a card in Workshop opens it in an padded container (card itself shouldn't be padded). Additionally, cards have duplicate "Open" and "Open on its own page" buttons. Opening a card on your own session navigates to the session instead of opening the card view. (Bruno, SNM, Evan) | One card component everywhere: same controls (reply, open, recent comments) in every view; remove the padded open container; merge the duplicate Open buttons; opening a card about your own session shows the card view, not the session. | Workshop |
| 8 | Session provider / Codex vs Claude tracking: Working directly from Codex shows up on the platform as a Claude session. (Bruno) | Record and display the actual provider/agent per session (Codex vs Claude). | Sessions |
| 9 | Proposal buttons: When working on proposals with multiple iterations, the "proposal" button only displays after the first iteration rather than staying at the bottom. (Bruno) | Keep the proposal button persistently at the bottom across all iterations. | Sessions |
| 10 | App creation iframe error: Creating a new app results in a "net::ERR_CONNECT_RESET" error on its iframe when opened. (Bruno) | Fix new-app provisioning/routing so a just-created app's iframe loads (likely same root as #6's spin-up). | Create/fork |
| 11 | Building with external tools & MCP setup: "Building with your own tools" guide is not working well with Codex, and instructions for setting up MCP are missing for Codex or other agents (only present for Claude, but currently broken). (Bruno) | Fix the broken Claude MCP instructions; add Codex (and generic-agent) setup instructions; verify the whole guide end to end with Codex. | Integrations |
| 12 | App access permissions and locking: All apps are currently locked under "account required" (even ones that didn't previously require an account), and logged-in waitlist users still cannot use any apps. In Workshop view, replace "app is locked" with a general message explaining who can build on the app. (Zura, Evan) | Restore per-app access (only truly account-required apps locked); let logged-in waitlist users use apps; reword the Workshop lock into "who can build here" messaging. | Auth |
| 13 | Deleting test apps: There is no way to delete a test app. (SNM) | Add app deletion for your own (test) apps, with a confirm step. | Create/fork |
| 14 | Error state UI/UX: When failing to load main/directory apps, it displays a generic "failed to load the apps" / "Couldn't reach the app directory" message that needs customized UI/UX alignment. (SNM) | Design a branded error state (illustration, plain-language copy, retry CTA) replacing the generic failure text; root cause tracked in #22. | Core UI |
| 15 | Issue creation workflow: The option to "create an issue" directly on the board is missing (currently under give feedback). (SNM) | Add "create an issue" directly on the board, not only inside give-feedback. | Workshop |
| 16 | Thread reply input layout: The "Reply in thread" input appears disconnected from the page on desktop browsers and is missing on workshop cards tapped under "needs your vote". (SNM, Evan) | Visually attach the reply input on desktop, and render it on needs-your-vote cards too. | Workshop |
| 17 | Assigned vs Claim: Improve UI/UX around "assigned" versus "claim". (SNM) | ❓ Make the two states visually distinct — clear "assigned to X" label vs. a single obvious Claim action on unclaimed items. See question below. | Workshop |
| 18 | Session action buttons: Opening a session no longer shows action buttons (e.g., archive). (SNM) | Restore the session action buttons (archive, etc.) — regression. | Sessions |
| 19 | Page layout consistency: Formatting differs between dev session and new session pages (one is full page, the other is not). (SNM) | Unify dev-session and new-session pages on one layout (rec: full page). | Sessions |
| 20 | Preview accuracy: Previews are sometimes inaccurate and fail to reflect features that have been worked on. (SNM) | ❓ Make previews reliably rebuild/refresh from the session's latest state. See question below (need repro examples). | Sessions |
| 21 | Voting remaining time: Remaining voting time is no longer displayed on tasks, making merge/discard status unclear. (SNM) | Restore the remaining-time display on votes so merge/discard timing is clear — regression. | Workshop |
| 22 | Appraise / API breakdown: SV APIs used to fetch app directory data are broken/incompatible (e.g. "Couldn't reach the app directory — try again in a moment"). (SNM) | Fix/re-version the app-directory APIs apps depend on (Appraise is the repro); add a compatibility guarantee so app-facing APIs don't silently break. | Infra |
| 23 | AI Permissions prompt: Recipe bot requests permission to use AI with no visible prompt or option to grant permission. (Lukas) | Show the AI-permission prompt with grant/deny whenever an app requests AI (recipe bot is the repro). | Apps |
| 24 | Create new app dialogue: Single-page dialogue shows all choices at once and should use the new pane styling; consider fewer choices or multi-step flow to avoid feeling overwhelming. (Lukas, Evan) | **DECIDE**: rec a multi-step flow (fewer choices per step) in the new pane styling. | Create/fork |
| 25 | Claude version error: Improving existing app triggers API Error 400 (Claude Code 2.1.245 does not support model; version 2.1.251+ required). (Lukas) | Upgrade the platform's Claude Code runner to ≥ 2.1.251. Blocker for improving apps. | Infra |
| 26 | Apps sorting & directory pagination: "Show more" only exists for "recommended" apps, while other sorting options show all. Consider adding a card/prompt in Discover for empty state ("nothing featured right now, browse the directory?"). (Evan) | Paginate consistently across all sort options. **DECIDE** (rec yes): add the empty-state card pointing to the directory. | Discover |
| 27 | Challenges UI & Leaderboard reorganization: Challenges are missing icons; missing top padding above completion status line; change "Open Leaderboard" link to "Open challenges" and add chevron arrows; reorganize Leaderboard/Challenges (Challenges -> Kudos -> Leaderboard, remove "Event/pre season 2" text and "See where season stands", use homepage styling and simplify Leaderboard standings view); tapping a challenge should give responsive click feedback and navigate directly to that dedicated challenge page. (Evan) | Add challenge icons + the missing padding; relabel to "Open challenges" with chevrons; reorder to Challenges → Kudos → Leaderboard, cut the season copy, match homepage styling, simplify standings; give taps press feedback that goes straight to the challenge page. | Challenges |
| 28 | Mobile panes & styling: Mobile panes (Notifications, Improve, App chips) still use old styling; browse all apps page needs new pane styling; back navigation from browse all apps shows overlapping app chips during animation. (Evan) | Apply the new pane styling to Notifications, Improve, app-chip panes and browse-all-apps; fix the overlapping-chip animation on back navigation. | Mobile |
| 29 | Workshop summary & stats: Workshop summary text erroneously repeats app stats, and hardcoded "20+ shipped this week" should show real numbers. Consider grounding the summary/experience socially by highlighting active contributors alongside changes. (Evan) | Fix the duplicated stats; replace hardcoded "20+ shipped" with live numbers. **DECIDE** (rec yes): surface active contributors in the summary. | Workshop |
| 30 | Voting delays & card statuses: Voting on a card in Workshop view takes 1-2 seconds before hiding from "needs your vote", and cards should separate code/conflict status from vote status. (Evan) | Hide voted cards optimistically (instant); show code/conflict state and vote state as two separate indicators. | Workshop |
| 31 | Discussion view cleanup: System messages in card discussion views are repetitively duplicated (e.g. "xyz is now synced with main and conflict free"). (Evan) | Deduplicate/collapse repeated system messages in card discussions. | Workshop |
| 32 | Mobile UI touch targets & press state: "Replying to" banner close button is too small on mobile, cards show active/pressed states while scrolling unlike standard iOS apps, and iOS system header vs platform background fade in/out at different rates when toggling dialogs. (Evan) | Enlarge the close tap target; suppress pressed states while scrolling (iOS convention); sync the header/background fades on dialog toggle (carried from 9/3 #62). | Mobile |
| 33 | Dev view controls & card typography: +button options in dev view should match desktop styling, and minor font/padding discrepancies exist between collapsed and expanded card views. (Evan) | Match the dev-view "+" menu to desktop styling; reconcile font/padding between collapsed and expanded cards. | Sessions |
| 34 | Board / Workshop view consolidated structure: Consider merging into Workshop view with toggle for "sort by category" vs "sort by status" (inline view switch); show auto-categories on board view; add "show more" button under "nobody has picked this up"; add fast board filters for "assigned to / created by you". (Evan) | **DECIDE**: rec merge Board into Workshop with an inline category/status sort toggle. Also: auto-categories on board view, "show more" under nobody-picked-this-up, quick filters for assigned-to-you / created-by-you. | Workshop |
| 35 | Mobile keyboard snapping: Typing on an issue hides past comments and causes the screen/keyboard to snap briefly after keypresses; typing in PWA sessions forces session pop-up while obscuring typed text. (Evan) | Fix keyboard/viewport handling: keep the thread visible and stable while typing; stop the PWA session pop-up from stealing focus and hiding typed text. | Mobile |
| 36 | Social account verification: Editing profile social accounts lacks ownership verification. (Evan) | Require ownership verification (OAuth or post-a-proof) before a social account appears on a profile. Depends on #46 working. | Integrations |
| 37 | Header details button: Revert top-right "details" button to show "built with" directly. (Evan) | Revert: show "built with" directly instead of the "details" button. | Core UI |
| 38 | Session UI layout & persistent indicators: Reduce height of session descriptor area, add persistent empty state text/layout (similar to Claude/ChatGPT), fix session detail bar turning white on scroll, fix broken Claude Code detail box toggle button/scrolling, and auto-set dark mode header when page content is dark. (Evan) | Shrink the session descriptor; add a persistent Claude/ChatGPT-style empty state; fix the detail bar whitening on scroll and the broken detail-box toggle/scrolling; auto-switch the header to dark when page content is dark. | Sessions |
| 39 | Status dots & session naming: Replace static green dot with active work indicator; clarify misleading yellow dot status; fix unclickable "handed off" session entries; auto-name default dev sessions (e.g. dev/evan-17890406…) based on first prompt message. (Evan) | Make the green dot a real activity indicator; define/clarify yellow; make "handed off" entries clickable; auto-name sessions from the first prompt. | Sessions |
| 40 | Server CSS issues: Missing CSS on new server for specific apps (todo list, clear skies, workquest). (Evan) | Fix asset build/serving for those three apps on the new server, then audit all apps for the same failure. | Infra |
| 41 | Homescreen search bar: Search bar is not full width or centered and disappears/snaps on homepage refresh. (Evan) | Make it full-width/centered and reserve its space on refresh (carried from 9/3 #64). | Core UI |
| 42 | Messages view pane UI: Clicking blank area below message selector should close compose/conversation pane; clicking message field should highlight whole input box; consolidate share/attachment icons; enable direct issue sharing via hamburger menu. (Evan) | Click-outside closes the pane; focus highlights the whole input; merge share/attachment into one icon; add issue sharing to the hamburger menu. | Messages |
| 43 | Live translation settings: Missing clear option/path to re-enable "live translation" after revoking it. (Evan) | Add a clear settings path to re-grant live translation (and any revoked app permission) after revoking. | Settings |
| 44 | Notifications & status updates: Improve sidebar update delay when spec finishes (and change label from "ready" to "ready for your input"). (Evan) | Update the sidebar promptly when a spec finishes; relabel "ready" → "ready for your input". | Core UI |
| 45 | Drafts management in sessions: Deleting drafts is broken, sending a draft does not remove it from saved drafts, and sending a draft populates input with an existing saved draft instead of clearing chat. (Evan) | Fix the draft lifecycle: delete works, sending removes the draft, and the composer clears instead of loading another saved draft. | Sessions |
| 46 | Third-party integrations (GitHub / X): GitHub and X account linking features are not working. (Evan) | Fix GitHub and X linking end to end (recurring — 9/3 #34/#50); add a regression test so it stays fixed. | Integrations |
| 47 | Session execution halts: Sending messages occasionally aborts abruptly with "thinking about your request" followed by "stopped by @user". (Evan) | Root-cause the spurious "stopped by @user" aborts — a session must only stop when the user actually stopped it. | Sessions |
| 48 | Missing models: GLM-5.3 is missing from available models. (Evan) | Restore GLM-5.3 to the model list — it's the intended default per the 9/3 decision (#89). | Models |
| 49 | Share links: Platform share links fail to open on Safari ("couldn't establish a secure connection"). (Evan) | Fix the TLS/cert setup on the share-link domain so Safari opens them; recheck after the #1 domain move. | Infra |

## Open questions (the ❓ rows)

- **#3** — Confirm the target: is solarized-light the intended palette (and the current tint the bug), or should we move off solarized entirely?
- **#17** — Assigned vs claim: what confused — the wording, not knowing who holds an item, or being able to claim something already assigned?
- **#20** — Preview accuracy: an example or two of a session whose preview was stale (which app/feature), so it's reproducible.
